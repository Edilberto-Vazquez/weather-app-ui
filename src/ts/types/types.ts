export type Location = {
  value: string;
  title: string;
};

export type Field = {
  value: string;
  title: string;
  selected: boolean;
};

export type Dates = {
  startDate: string;
  endDate: string;
};
